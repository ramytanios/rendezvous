[project]
name = "tui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "textual>=6.7.1",
    "textual-dev>=1.8.0",
    "websockets>=15.0.1",
]
